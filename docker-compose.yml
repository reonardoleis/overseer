version: "3.5"
services:
  app:
    image: overseer/latest
    environment:
      - OPENAI_KEY=${OPENAI_KEY}
      - DISCORD_TOKEN=${DISCORD_TOKEN}
    volumes:
      - ${FUNCTIONS_PATH}:/app/functions
      - ${AUDIOS_PATH}:/app/audios
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: "100MB"
